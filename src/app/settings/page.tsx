import SettingsClient from './client';

export default async function SettingsPage() {
  // In the future, we might fetch user settings here.
  // const userSettings = await getSettings();
  return <SettingsClient />;
}
